import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center justify-center">
        <span className="loading loading-xl flex items-center justify-center loading-spinner text-info"></span>
    </div>
  );
};

export default Loading;
